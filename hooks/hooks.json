{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Check the edited file. If it is not in scenes/ — return {\"ok\": true} immediately, no further checks.\n\nIf the file is in scenes/, check the content that was just written (no need to read external files):\n\n**One slug per file (fiction):** count lines starting with `INT.` or `EXT.` (not inside a heading or table). If more than one slug line is found — return {\"ok\": false, \"reason\": \"Multiple slug lines detected. Split this file now: each slug becomes its own scene file. Renumber subsequent scenes to accommodate the new files. Do not ask for confirmation.\"} immediately, no further checks.\n\n**Format — Fiction** (heading starts with '# Scene'): slug lines plain uppercase (INT./EXT., no asterisks), character names plain uppercase (NAME, no asterisks), exactly one blank line between blocks (action → name, dialogue → next action); NEVER two blank lines before a character name; no blank line between name and dialogue.\n\n**Format — Documentary** (heading starts with '# Block'): table with | VIDEO | AUDIO | columns, V/O and SOT labels bold, NAT SOUND italic in parentheses.\n\n**AI patterns** — flag if present: emotional placeholders (\"looks thoughtfully\", \"sighs meaningfully\", \"something shifts in his eyes\"), dialogue that explains feelings (\"I'm angry because...\"), exactly 3 adjectives in a row (rule of threes), filler phrases (\"it's important to note\", \"plays a vital role\").\n\n**Action blocks (fiction):** action lines must not be written as one solid paragraph. Each beat — its own paragraph, separated by a blank line. Flag if two or more distinct actions are merged into a single paragraph without a blank line between them.\n\n**Value shift (fiction):** does something change? Entry state → exit state on any axis: safety/danger, hope/despair, connection/isolation, truth/lie, power/powerlessness. If the character ends in the same state they entered — flag it.\n\n**Productive block (documentary):** does the block reveal new information, complicate, or answer something? If it only repeats what the audience already knows — flag it.\n\nReturn {\"ok\": true} if no issues. Return {\"ok\": false, \"reason\": \"specific issue with scene/line reference\"} if issues found. One reason only — the most critical."
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python \"${CLAUDE_PLUGIN_ROOT}/hooks/compass_check.py\""
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python \"${CLAUDE_PLUGIN_ROOT}/hooks/session_report.py\""
          }
        ]
      }
    ]
  }
}
